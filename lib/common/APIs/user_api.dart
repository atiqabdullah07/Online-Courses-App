import 'package:blocapp/Models/user_model.dart';

class UserAPI {
  login({LoginRequestEntity? param}) {
    // var response = await HttpUtil().post(
    //   'api/login',
    //   queryParameters: param.toJson()
    // )
  }
}
